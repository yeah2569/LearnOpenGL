1. intro
   1) create Buffer ID by glGenBuffers
   2) bind Buffer obj by glBindBuffer
   3) request Buffer zone by glBufferData
   4) special the Buffer's func,example:glVertexPoint special the address and glEnableClientState(GL_VERTEX_ARRAY) special the func
   5) active the buffer by glBindBuffer
   6) special the render model example : glDrawArrays() or glDrawElements() etc.
